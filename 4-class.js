class calculator {
  static sum(x, y) {
    return x, y;
  }

  sum2(x, y) {
    return x + y;
  }

  multiply(x, y) {
    return x * y;
  }
}

const calculatororodject = new calculator();

console.log(calculator.sum(1, 2));
console.log(calculatororodject.sum2(2, 3));
// console.log(calculatororodject).sum(5, 6);
console.log(calculatororodject.multiply(10, 20));
